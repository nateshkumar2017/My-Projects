# Age and Gender Detection with OpenCV & Tkinter

## Prerequisites
You’ll need to install OpenCV (cv2) to be able to run this project. You can do this with pip

``` bash
pip install opencv-python

```

